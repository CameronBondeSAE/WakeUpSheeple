using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RadiationZone : MonoBehaviour
{
    
    // Update is called once per frame
    void Update()
    {
        //apply damage over time to sheep caught within the zone
    }
}
